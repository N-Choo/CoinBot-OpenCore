[package]
name = "trade-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../../lib/common" }
share = { path = "../../lib/share" }
tokio = { version = "1", features = ["full"] }
